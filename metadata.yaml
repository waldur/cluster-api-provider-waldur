apiVersion: clusterctl.cluster.x-k8s.io/v1alpha3
releaseSeries:
  - major: 0
    minor: 1
    # NOTE: the provider implements the v1beta2 contract in code and requires CAPI core >= v1.9.0.
    # v1beta1 is declared here to pass Turtles' pre-flight check, which rejects v1beta2 providers
    # even on CAPI operator versions that fully support them. Safe to change to v1beta2 once
    # Turtles' validation is updated.
    contract: v1beta1

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  labels:
    cluster.x-k8s.io/v1beta2: v1beta2
  name: waldurmachines.infrastructure.cluster.waldur.com
spec:
  group: infrastructure.cluster.waldur.com
  names:
    kind: WaldurMachine
    listKind: WaldurMachineList
    plural: waldurmachines
    singular: waldurmachine
  scope: Namespaced
  versions:
  - name: v1beta2
    schema:
      openAPIV3Schema:
        description: WaldurMachine is the Schema for the waldurmachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WaldurMachine
            properties:
              dataDiskSize:
                description: DataDiskSize is the size in GB of the data disk. Applicable
                  to worker nodes.
                minimum: 10
                type: integer
              flavor:
                description: Flavor is the OpenStack flavor name for this machine.
                type: string
              image:
                description: Image is the name or UUID of the OS image to use for
                  this VM.
                type: string
              nodeType:
                description: NodeType classifies this machine as a worker or storage
                  node.
                enum:
                - worker
                - storage
                type: string
              offeringSlug:
                description: |-
                  OfferingSlug identifies which datacenter offering (OpenStack deployment) this
                  machine should be provisioned in. Must match one of the offering slugs in the
                  parent WaldurCluster's spec.datacenters.
                type: string
              providerID:
                description: |-
                  ProviderID is the identifier of the VM in the cloud provider, set after provisioning.
                  Follows the format "waldur://<vm-uuid>". Required by the CAPI Machine controller.
                type: string
              vsanDiskSize:
                description: VsanDiskSize is the size in GB of the vSAN disk. Required
                  for storage nodes.
                minimum: 100
                type: integer
            required:
            - flavor
            - image
            - nodeType
            - offeringSlug
            type: object
          status:
            description: status defines the observed state of WaldurMachine
            properties:
              conditions:
                description: conditions represent the current state of the WaldurMachine
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              initialization:
                description: |-
                  Initialization provides information about machine provisioning completion.
                  Required by the CAPI v1beta2 contract.
                properties:
                  provisioned:
                    description: Provisioned indicates the machine infrastructure
                      has been provisioned.
                    type: boolean
                type: object
              marketplaceResourceState:
                description: MarketplaceResourceState is the lifecycle state of the
                  marketplace resource.
                type: string
              marketplaceResourceUuid:
                description: |-
                  MarketplaceResourceUuid is the UUID of the marketplace resource backing this VM.
                  Required to submit a termination order.
                type: string
              order:
                description: Order is the currently executing or most recent Waldur
                  order for this VM.
                properties:
                  marketplaceResourceUuid:
                    type: string
                  resourceUuid:
                    type: string
                  state:
                    description: OrderState defines model for OrderState.
                    type: string
                  type:
                    description: RequestTypes defines model for RequestTypes.
                    type: string
                  uuid:
                    type: string
                type: object
              state:
                description: State is the current Waldur resource lifecycle state
                  of the VM.
                type: string
              vmUuid:
                description: VmUuid is the UUID of the VM in Waldur/OpenStack, set
                  after provisioning.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
